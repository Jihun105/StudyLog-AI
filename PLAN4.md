# StudyLog-AI — Phase 11 진행 상황

> `PLAN3.md`의 후속 문서. Phase 10(할 일 시간표/종료시간/날짜변경, 전체 폴더보기, 태그 칩 입력, 글쓰기 페이지 사이드바 전환)까지는
> `PLAN3.md`에 정리되어 있고 커밋됨(`55d7dc0`). 이 문서는 그 이후, 노트 작성 화면을 Apple Pages 스타일로
> 개편하며 진행된 작업들을 정리함.

---

## 전체 구현 계획 및 진행 상황

| Phase | Step | 내용 | 상태 |
|---|---|---|---|
| Phase 11 | UI | 노트 작성 화면에 얇은 상단 툴바 + 우측 "서식" 패널 추가 | ✅ 완료 |
| Phase 11 | UI | 노트 작성 화면에 "목차" 패널 추가 (제목 클릭 시 해당 위치로 이동) | ✅ 완료 |
| Phase 11 | UI | AI 이어쓰기(초안 작성 보조) 기능 | 🔄 추가했다가 요청으로 다시 제거 |
| Phase 11 | UI | 글쓰기/수정 페이지 카테고리 선택 — 브라우저 기본 select → 커스텀 드롭다운 | ✅ 완료 |
| Phase 11 | UI | 상단 툴바 재배치 — "서식"/"목차" 라벨 노출 + 구분선(divider) 정렬 | ✅ 완료 |
| Phase 11 | UI | 서식 패널에 "구분선"(수평선) 삽입 기능 추가 | ✅ 완료 |
| Phase 11 | UX | 노트 편집란 빈 화면에 `'/'를 입력해 명령어 사용` 안내 문구 표시 | ✅ 완료 |
| Phase 11 | UX | 할 일 "미루기" 버튼 문구를 "날짜변경"으로 변경 | ✅ 완료 (사소한 수정) |

---

## 노트 작성 화면 — Apple Pages 스타일 개편

노트를 쓰는 화면(`PostCreatePage`/`PostEditPage`)을 Apple Pages 참조해서, 회색 캔버스 위에 흰 "문서 페이지" 카드가 떠 있는 형태로 개편하고, 에디터(`RichTextEditor`)에는 다음 3가지 기능을 추가함.

- **상단 툴바**: 표 삽입 / 이미지 삽입 아이콘 + "서식" 패널 토글 + "목차" 패널 토글. 아이콘만으로는 뭔지 알기 어렵다는 피드백을 받아, "서식"은 툴바 맨 왼쪽에, "목차"는 맨 오른쪽에 글자를 같이 보여주도록 배치하고, "서식"과 표/이미지 아이콘 사이에 세로 구분선을 넣어 구역을 나눔.
- **서식 패널**: 화면 오른쪽에 뜨는 패널로, 지금 커서 위치의 블록 종류(본문/H1~H3/글머리·번호·체크 목록/구분선)와 텍스트 서식(굵게/기울임/밑줄/취소선), 정렬(왼쪽/가운데/오른쪽)을 바로 바꿀 수 있음. 평소엔 접혀 있다가 토글을 누를 때만 나타남.
- **목차 패널**: 문서 안의 제목(H1~H3)들을 모아서 보여주고, 클릭하면 해당 위치로 스크롤 이동.
- **구분선(수평선) 삽입**: 서식 패널의 스타일 목록에 추가. 처음엔 직접 커스텀 블록(`<hr>`)을 만들었는데 클릭해도 화면에 아무것도 안 보이는 렌더링 버그가 있어서, BlockNote에 이미 내장되어 있고 실제로 점선을 그려주는 `pageBreak` 블록(원래는 인쇄용 페이지 나누기 기능)을 재사용하는 방식으로 교체함.
- **AI 이어쓰기 기능**: 지금까지 쓴 글을 이어서 짧게 제안해주는 기능을 만들었다가("AI로 이어쓰기" 버튼 + 백엔드 `/api/ai/continue-writing` 엔드포인트), 곧바로 제거 요청이 있어 프론트/백엔드 모두 원상복구함. 최종 코드에는 남아있지 않음.
- **마크다운 단축키 / "/" 슬래시 메뉴**: 별도로 만든 기능은 아니고, BlockNote 라이브러리에 원래 내장되어 있는 기능임을 확인함. `#`/`##`/`###`(제목), `-`/`*`(글머리 기호), `1.`(번호 매기기), `>`(인용), `[ ]`(체크리스트), ` ``` `(코드 블록) 등이 입력만으로 자동 변환되고, 빈 블록에서 "/"를 입력하면 슬래시 메뉴가 뜸.

**변경 파일**
- `frontend/src/components/RichTextEditor.jsx` — 상단 툴바, 서식 패널, 목차 패널, 구분선 삽입 로직, 커스텀 스키마(`withPageBreak`) 구성
- `frontend/src/pages/PostCreatePage.jsx`, `PostEditPage.jsx` — 캔버스형 레이아웃(회색 배경 + 흰 문서 카드), 상단 sticky 헤더
- `frontend/src/i18n/locales/{ko,en}.json` — `richTextEditor.*` 신규 키 다수(`insertTable`, `insertImage`, `formatPanel`, `style`, `divider`, `tableOfContents` 등)

## 글쓰기/수정 페이지 — 카테고리 선택 커스텀 드롭다운

브라우저 기본 `<select>`는 옵션 목록에 Tailwind 스타일이 전혀 적용되지 않아 다크모드에서 특히 못생기게 나오는 문제가 있어, 폴더 트리(들여쓰기 + 폴더 아이콘)를 그대로 보여주는 커스텀 드롭다운으로 교체함.

- `frontend/src/components/CategorySelect.jsx` (신규) — 버튼 클릭으로 열리는 목록형 드롭다운. 폴더 아이콘 + 깊이별 들여쓰기 + 선택 시 파란색 하이라이트, 바깥 클릭 시 자동 닫힘
- `frontend/src/pages/PostCreatePage.jsx`, `PostEditPage.jsx` — 기존 `<select>`를 `CategorySelect`로 교체

## 그 외 소소한 수정

- 에디터가 완전히 비어있을 때 뜨는 안내 문구를 `"텍스트를 입력하거나 '/'..."` 에서 `"'/'를 입력해 명령어 사용"`으로 단순화 (영문: `"Type '/' for commands"`)
- 할 일 세부사항의 "미루기" 버튼 문구를 "날짜변경"으로 변경

---

## 검증

이번 배치는 `mcp__workspace__bash`를 통한 babel 문법 검사가 세션 내내 실제 파일 내용과 다른 결과(존재하지 않는 줄 번호의 오류)를 반환하는 문제가 있어 — 이 세션에서만 최소 5회 이상 재현된, 파일마다 반복적으로 나타난 샌드박스 자체의 알려진 캐싱/오염 버그로 판단됨 — babel 결과 대신 `Read` 도구로 저장된 파일 내용을 직접 재검토하는 방식으로 검증함(모든 함수/JSX 태그 짝 맞음, import 누락 없음 확인). `git diff`도 같은 세션에서 오래된 내용을 보여주는 현상이 있어 신뢰하지 않고 배제함.

**로컬에서 직접 실행해서 아래 항목들이 실제로 정상 동작하는지 확인이 필요함:**
- 서식 패널의 "구분선" 버튼 클릭 시 점선이 실제로 삽입되는지
- 카테고리 커스텀 드롭다운에서 폴더 선택 후 저장이 정상적으로 되는지
- 목차 패널에서 제목 클릭 시 해당 위치로 스크롤 이동하는지
- 서식 패널 스타일 그리드가 2행(8칸)으로 잘 정렬되어 보이는지

이번 배치는 아직 git에 커밋되지 않은 상태임. `git add`/`commit`은 다음 배치 작업과 함께 정리해서 진행하는 게 좋음(현재 워킹 디렉토리에 이전 세션들에서 남은 줄바꿈(CRLF/LF) 노이즈성 변경사항이 다수 섞여 있어, 실제 변경 파일만 골라 스테이징해야 함 — `git diff -w`로 구분).
