# NEXT.md — 다음 단계 로드맵

> 2026-07-09 논의 내용 정리. 아직 구현 전, 방향만 확정한 상태.

## 우선순위 요약

1. 관리자 대시보드
2. 소개/문의 페이지
3. 점검모드
4. 저장 실패로 인한 데이터 유실 방지 (가장 시급)
5. Quiz 고도화 — 보류
6. 리소스 비용 최소화
7. 모바일 앱 배포 — 모든 기능 완료 후 마지막에 진행

---

## 1. 관리자 대시보드

- **인증**: 기존 `User` 테이블에 `is_admin` 플래그 컬럼 추가. 별도 계정 체계를 만들지 않고, 본인 계정에 플래그를 켜서 로그인 후 관리자 메뉴가 추가로 보이는 방식.
- **접속 현황**: 웹소켓 기반 실시간 온라인 표시. 서버 메모리에 현재 연결된 사용자 목록을 관리 (단일 EC2 인스턴스라 별도 메시지 브로커 불필요. 추후 서버가 여러 대로 늘어나면 Redis pub/sub 등으로 공유 필요).
- **OpenAI 사용량 추적**: OpenAI 호출할 때마다 응답에 포함된 토큰 사용량을 자체 DB에 기록 → 기능별/월별 예상 비용을 관리자 화면에서 확인. 나중에 OpenAI 공식 Usage/Cost API(Admin API 키 필요)를 추가해 실제 청구 금액과 비교하는 것도 가능 (독립적으로 나중에 추가 가능, 지금 구조를 바꿀 필요 없음).
- **화면 위치**: 기존 React 프런트엔드에 관리자 전용 보호 라우트로 추가 (예: `/admin-dashboard`). 기존 Django admin 서비스나 완전 별도 앱은 채택하지 않음.

## 2. 소개/문의 페이지

- **문의 처리**: 문의 폼 제출 → 백엔드가 DB에 저장 → 이메일 알림 발송(Resend 등 무료 티어 이메일 API) → 관리자 대시보드의 "문의함"에서 확인.
- **소개 페이지**: 실제 서비스 사용자 대상 페이지. 서비스 한 줄 소개, 핵심 기능(노트 작성, AI 퀴즈 생성, 캘린더/할일 관리, AI 시맨틱 검색 등)을 사용자 관점에서 설명, 필요시 간단 사용법/FAQ.
- **포트폴리오용 기술 소개(스택, 아키텍처 등)는 이 페이지에 넣지 않고, 별도로 GitHub README에 작성** — 전체 기능 구현이 어느 정도 정리된 후 별도 요청 시 작성.

## 3. 점검모드

- **적용 지점**: nginx 레벨. nginx가 백엔드로 요청을 넘기기 전에 "점검 중" 플래그 파일 존재 여부를 확인하고, 있으면 백엔드를 거치지 않고 바로 점검 페이지를 반환. 백엔드 자체가 완전히 죽어도 (예: 이번에 겪은 DB 스키마 불일치 사고) 사용자는 항상 정상적인 점검 페이지를 보게 됨.
- **구조**: 백엔드와 nginx(frontend 컨테이너)가 플래그 파일을 공유할 수 있도록 도커 볼륨 구성 필요.
- **자동화**: CI/CD 배포 스크립트(`.github/workflows/deploy.yml`)에 연동 — 배포 시작 시 점검모드 자동 ON, 마이그레이션/빌드 성공 시 자동 OFF, 실패 시 점검모드 유지(원본 에러가 사용자에게 노출되지 않도록).
- **수동 토글**: 관리자 대시보드에서도 수동으로 켜고 끌 수 있게 지원.

## 4. 저장 실패로 인한 데이터 유실 방지 (최우선)

배경: 친구가 1시간 동안 작성한 글이 저장 실패로 전부 유실된 사고.

- **자동 임시저장**: 에디터에서 타이핑하는 동안 몇 초 간격으로 브라우저 `localStorage`에 현재 내용을 계속 저장. 서버 저장 실패/새로고침/탭 닫힘 등 어떤 상황에서도 마지막 저장 시점까지 복구 가능.
- **이탈 경고**: 저장되지 않은 내용이 있는 상태에서 새로고침/다른 페이지 이동/탭 닫기 시 브라우저 경고(beforeunload) 표시.
- **스킵한 것**: 별도의 "저장 실패 → 재시도 버튼 UI"는 만들지 않기로 함 (자동 임시저장이 있으면 충분, 실패해도 사용자가 다시 저장 버튼을 누르면 됨). 다만 구현 시 저장 실패 상황에서 에디터 내용이 지워지거나 페이지가 강제 이동되는 기존 버그가 있는지는 확인 후 수정.

## 5. Quiz 고도화 (보류)

방향 후보만 정리, 어느 걸 할지는 추후 더 고민 후 결정:

- 오답 복습 시스템 (spaced repetition, Anki 방식)
- 오답에 대한 AI 설명/피드백
- 학습 분석 대시보드 (과목별 정답률 추이, 취약 주제 등 — 관리자 대시보드의 차트 작업과 기술적으로 연계 가능)
- 문제 유형 다양화 (서술형 채점, 매칭 문제 등)
- 난이도 자동 조절

## 6. 리소스 비용 최소화

진행 순서: 알림 설정(무비용, 즉시 가능) → 실사용 데이터 확보 → 최적화 지점 판단.

- AWS Billing Alarm 설정 (콘솔에서 바로 가능)
- OpenAI 계정 사용량/예산 알림 설정 (콘솔에서 바로 가능)
- OpenAI 호출 최적화: 불필요한 재임베딩/중복 호출 여부 점검 및 캐싱
- EC2 실제 CPU/메모리 사용률 확인 후 인스턴스 사이징 재검토
- 장기 운영 확정 시 Reserved Instance/Savings Plan 전환 검토

## 7. 모바일 앱 배포 (마지막 단계)

전제: 웹 기능이 전부 안정화된 이후에 진행. 지금은 방향만 확정.

- **기술 선택**: React Native (Capacitor 대신 진짜 네이티브 앱으로 결정).
- **공유 vs 신규 제작**:
  - 공유: API 호출 로직(`frontend/src/api/*.js`), 데이터 모델, 인증 로직 — 모노레포 구조로 공통 패키지화 고려.
  - 신규 제작: UI는 기존 웹 컴포넌트를 그대로 옮기지 않고 모바일 전용으로 새로 구현.
- **단계적 출시**: 처음부터 전체 기능 이식하지 않고, 핵심 기능(노트 목록 조회, 할일/캘린더 확인, 간단 메모 작성) 먼저 출시 → 리치 에디터 등 복잡한 기능은 이후 단계 또는 웹 전용으로 유지.
- **선행 조건**: 백엔드 HTTPS 적용 필수 (현재 `http://15.165.35.74` 평문 IP 접속 — iOS/Android 앱은 기본적으로 일반 HTTP 통신 차단). 도메인 구매 + 무료 인증서(Let's Encrypt 등) 적용 필요.
- **추가 비용**: 도메인 비용(연 1만원대), Apple 개발자 프로그램(연 $99), Google Play 개발자 등록(최초 1회 $25).
